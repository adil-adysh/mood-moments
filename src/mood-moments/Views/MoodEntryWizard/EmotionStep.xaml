<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:mood_moments.ViewModels"
             x:Class="mood_moments.Views.MoodEntryWizard.EmotionStep"
             x:Name="EmotionStepRoot"
             x:DataType="viewModels:NewEntryWizardViewModel">
    <StackLayout Padding="10" Spacing="15">
        <!-- Main Title -->
        <Label Text="{Binding StepTitle}"
                FontSize="22"
                FontAttributes="Bold"
               SemanticProperties.HeadingLevel="Level1"
               SemanticProperties.Description="Emotion Selection"/>

        <Label Text="Select one of the options below"
                FontSize="18"
                FontAttributes="Bold"
               SemanticProperties.HeadingLevel="Level2"
               SemanticProperties.Description="Emotion Options"/>

        <!-- Core Emotions -->
        <CollectionView ItemsSource="{Binding EmotionSelection.CoreEmotions}" SelectionMode="Single" x:Name="CoreEmotionsCollection" SemanticProperties.Description="Core emotion options">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <RadioButton Content="{Binding .}"
                                 GroupName="CoreEmotionsGroup"
                                 CheckedChanged="OnCoreEmotionCheckedChanged"
                                 SemanticProperties.Description="{Binding .}"
                                 AutomationId="{Binding .}" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Text="Skip to Intensity"
                Command="{Binding SkipToIntensityCommand}"
                Margin="0,10,0,0"
                HorizontalOptions="Center"
                IsEnabled="{Binding EmotionSelection.SelectedCoreEmotion, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                SemanticProperties.Description="Skip to intensity selection"
                AutomationId="SkipToIntensityButton" />
    </StackLayout>
</ContentView>
