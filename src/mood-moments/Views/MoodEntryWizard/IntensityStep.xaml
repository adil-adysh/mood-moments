<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:mood_moments.ViewModels"
             x:Class="mood_moments.Views.MoodEntryWizard.IntensityStep"
             x:Name="IntensityStepRoot">
    <ContentView.BindingContext>
        <viewModels:NewEntryWizardViewModel />
    </ContentView.BindingContext>
    <StackLayout>
        <Label Text="Step 4: Intensity" FontSize="22" FontAttributes="Bold"
               SemanticProperties.HeadingLevel="Level1"
               SemanticProperties.Description="Intensity Step Heading" />
        <CollectionView ItemsSource="{Binding IntensityLabels}" SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Button Text="{Binding .}" Command="{Binding BindingContext.SelectIntensityCommand, Source={x:Reference Name=IntensityStepRoot}}" CommandParameter="{Binding .}" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentView>
